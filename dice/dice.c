#include "dice.h"

char easyDie[6] = "b,b,b,f,f,s";
char mediumDie[6] = "b,b,s,s,f,f";
char hardDie[6] = "s,s,s,b,f,f";
//
//prepare cup
bool prepare_cup() 
{
    cup_t cup[];
    for(int i = 0; i < TOTAL_DICE; i++)
    {
        cup[]
    }    
    if cup[] == 
        return true;
}

//count dice
int count_dice() 
{
    //int easyCount = 0; //is this public? answer: yes, because it's in the header file
    //int mediumCount = 0; //do these need to be declared here and initialized to 0? answer: no
    //int hardCount = 0;
    for(int i = cup[]; i < WINSCORE_MAXDICE; i++)
    {
        if(cup[i] == 'e')
        count++;
    }
    return count;
}